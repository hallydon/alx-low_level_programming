#include <stdio.h>
/**
 * main -prints the name of the file if the program
 * was compiled from , followed by new line
 * Return: Always 0
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
